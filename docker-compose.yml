services:
  copilot-proxy:
    build: .
    ports:
      - "${PORT:-8779}:8000"
    environment:
      - OPENAI_BASE_URL=${OPENAI_BASE_URL}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
